// Pequeño toque de UX: confirmación visual en botones del carrito (sin romper nada)
document.addEventListener("click", (e) => {
  if (e.target.matches(".btn")) {
    // no obligatorio; solo para sensación de interacción
  }
});
